一、分别以debugger模式启动eureka-01-server-8000 服务端和eureka-02-provider-8081 客户端 服务器
	查看启动情况：
		http://localhost:8000/
		http://localhost:8081/actuator/health

二、InstanceResource.java类的 public Response deleteStatusUpdate( 方法里打上断点

三、使用postman访问如下接口（POST方式）
	服务下线（请求头为：Content-Type: application/json）：
		http://localhost:8081/actuator/serviceregistry
			请求体为：
				{
					"status":"CANCEL_OVERRIDE"
				}
				
三、debugger并进行查看

四、查看eureka-02-provider-8081服务器运行状态和http://localhost:8000/ eureka中的Application的状态