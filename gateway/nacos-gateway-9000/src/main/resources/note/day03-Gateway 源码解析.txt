先决条件：
	1、解开 application.yml --》# day01：gateway 配置方式 下的gateway配置信息
	2、注释 NacosGateway9000Application.java 类的 // day01：gateway API 方式 下的注解
	
关键类：
	DispatcherHandler.java、WebHandler.java、Mono.java、ServerWebExchange.java

测试：
	1、DispatcherHandler.java类的 return Flux.fromIterable(this.handlerMappings) 处打上断点
	
	2、DispatcherHandler.java类的 .concatMap(mapping -> mapping.getHandler(exchange)) 处打上断点
		2.1：跟踪 RoutePredicateHandlerMapping.java 这个mapping
		
	3、DispatcherHandler.java类的 .flatMap(handler -> invokeHandler(exchange, handler)) 处打上断点
		3.1：跟踪 SimpleHandlerAdapter.java 这个适配器

	4、postman访问（GET方式,header头部信息为：X-Request-Id:123）：
		http://localhost:9000/info/param
		
	5、debugger并进行查看
