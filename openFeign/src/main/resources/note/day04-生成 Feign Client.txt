一、FeignClientFactoryBean.java类的 public Object getObject() throws Exception { 方法里打上断点

二、FeignClientFactoryBean.java类的 <T> T getTarget() { 方法里打上断点

三、debugger模式启动启动 nacos-03-consumer-feign-8080

四、切换DepartService.java类的@FeignClient的连接方式进行测试
				
五、debugger并进行查看

六、FeignClientFactoryBean.java类的 return (T) targeter.target(this, builder, context, 处打上断点，且切换DepartService.java类的@FeignClient的连接方式为直连方式

七、debugger模式启动启动 nacos-03-consumer-feign-8080

八、debugger并进行查看
