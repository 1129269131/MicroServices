关闭Spring Cloud LoadBalancer 负载均衡：
	1、nacos-03-consumer-feign-8080 --》resources --》application.yml 注释Spring Cloud LoadBalancer 负载均衡相关配置：
		loadbalancer:
			ribbon:
				enabled: false
	2、nacos-03-consumer-feign-8080 --》pom.xml --》注释 <artifactId>spring-cloud-starter-loadbalancer</artifactId>
	3、切换DepartService.java类的@FeignClient的连接方式为直连方式
	

一、FeignClientFactoryBean.java类的 public Object getObject() throws Exception { 方法里打上断点

二、FeignClientFactoryBean.java类的 <T> T getTarget() { 方法里打上断点

三、FeignClientFactoryBean.java类的 return (T) targeter.target(this, builder, context, 处打上断点

四、debugger模式启动启动 nacos-03-consumer-feign-8080
				
五、debugger并进行查看
